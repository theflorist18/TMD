__pycache__/
*.py[cod]
*$py.class
.Python
debug-*.log
.venv/
venv/
env/
# Local Vite overrides (API URL, dev flags — never commit real secrets)
web/.env
web/.env.local
web/.env.*.local
.env.local

# GitNexus (local graph index; do not commit)
.gitnexus/

# Root devDependencies (gitnexus CLI)
node_modules/

# Vite (web/)
web/node_modules/
web/dist/

# Generated datasets — run `python scripts/build_profiles.py` after clone
output/*
!output/.gitkeep
# Allow the main ownership table in git so GitHub Pages CI can copy it into dist (optional).
!output/one_percent_holders.csv
# Intelligence tab (GitHub Pages): commit if you want them public; watch GitHub file size limits.
!output/investor_profiles.json
!output/investor_groups.json
!output/investor_group_candidates.json
!output/investor_profiles_summary.json
!output/free_float.json
